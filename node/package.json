{
  "name": "node-cpp-addons-boilerplate",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build-addons": "node-gyp configure && node-gyp build",
    "start": "npm run build-addons && node app.js"
  },
  "description": "Boilerplate for requiring your c++ code into node.js",
  "dependencies": {
    "express": "^4.14.0"
  }
}
